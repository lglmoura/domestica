= simple_form_for(@empregador) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nome
    = f.input :endereco, label: "Endereço"
    = f.input :numero

  .form-actions
    = f.submit "Salvar"
